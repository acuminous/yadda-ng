const AsyncFunction = require('./AsyncFunction');
const CallbackFunction = require('./CallbackFunction');
const PendingFunction = require('./PendingFunction');

module.exports = {
  AsyncFunction,
  CallbackFunction,
  PendingFunction,
};
